## tech stack

-- use tailwindcss for styles
-- try not to add any unnecessary packages other than what is inside package.json

## guidelines

-- try not to use colors which are not already in the project
-- don't use divs if we have components already
-- components are in src/components/
