import React from "react";

const Header: React.FC = () => {
  return (
    <div className="border border-black p-2 flex justify-center items-center">
      <h1>Tablica odjazdów {}</h1>
    </div>
  );
};

export default Header;
